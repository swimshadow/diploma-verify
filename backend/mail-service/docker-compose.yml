services:
  mail-service:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SMTP_HOST=smtp.gmail.com
      - SMTP_PORT=587
      - SMTP_USER=jurasyn2008@gmail.com
      - SMTP_PASSWORD=ibqjinghxewuzzfa
      - SMTP_USE_TLS=true
      - DEFAULT_SENDER=jurasyn2008@gmail.com
      - BASE_URL=http://localhost:8000
    restart: unless-stopped
