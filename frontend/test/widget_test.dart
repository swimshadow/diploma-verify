import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder – real widget tests will be added later
    expect(1 + 1, equals(2));
  });
}
